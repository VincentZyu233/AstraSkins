# 🧾 自动识别文本文件，并统一使用 LF 换行符，避免跨平台提交产生无意义差异
* text=auto eol=lf

# 📊 将所有 Markdown 文档纳入 GitHub Linguist 语言统计
*.md linguist-detectable=true
# 📖 明确将各语言 README 识别为 Markdown，并按源代码而非文档类文件计入统计
README*.md linguist-language=Markdown linguist-detectable=true linguist-documentation=false

# 🧩 将编译后的动态链接库标记为二进制文件，禁止文本差异和换行转换
*.dll binary
# 🐧 将 Linux 共享库标记为二进制文件，禁止文本差异和换行转换
*.so binary
# 🐞 将调试符号文件标记为二进制文件，避免 Git 尝试生成文本差异
*.pdb binary
# 🖼️ 将 PNG 图片标记为二进制文件，保持原始字节内容
*.png binary
# 🖼️ 将 JPG 图片标记为二进制文件，保持原始字节内容
*.jpg binary
# 🖼️ 将 JPEG 图片标记为二进制文件，保持原始字节内容
*.jpeg binary
# 🖼️ 将 WebP 图片标记为二进制文件，保持原始字节内容
*.webp binary
